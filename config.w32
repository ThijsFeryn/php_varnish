// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("varnish", "for varnish support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("varnish", "enable varnish support", "no");

if (PHP_VARNISH != "no") {
	EXTENSION("varnish", "varnish.c");
}

